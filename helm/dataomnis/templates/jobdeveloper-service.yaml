apiVersion: v1
kind: Service
metadata:
  name: {{ include "dataomnis.fullname" . }}-jobdeveloper
  labels:
{{ include "dataomnis.labels" . | indent 4 }}
    component: jobdeveloper-service
spec:
  type: ClusterIP
  ports:
    - name: service
      port: {{ .Values.ports.jobdeveloper }}
      targetPort: service
      protocol: TCP
    - name: metrics
      port: {{ .Values.metricsPorts.jobdeveloper }}
      targetPort: metrics
      protocol: TCP
  selector:
    app.kubernetes.io/name: {{ .Chart.Name }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    component: jobdeveloper
