apiVersion: v1
kind: Service
metadata:
  name: {{ include "databench.fullname" . }}-udfmanager
  labels:
{{ include "dataworkbench.labels" . | indent 4 }}
    component: udfmanager-service
spec:
  {{- if .Values.udfmanager.service }}
  type: {{ coalesce .Values.udfmanager.service.type .Values.common.service.type }}
  {{- else }}
  type: {{ .Values.common.service.type }}
  {{- end }}
  ports:
    - name: service
      port: {{ .Values.ports.udfmanager }}
      targetPort: service
      protocol: TCP
  selector:
    app.kubernetes.io/name: {{ .Chart.Name }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    component: udfmanager
