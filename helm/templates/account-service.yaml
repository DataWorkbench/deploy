apiVersion: v1
kind: Service
metadata:
  name: {{ include "databench.fullname" . }}-account
  labels:
{{ include "databench.labels" . | indent 4 }}
    component: account-service
spec:
  {{- if .Values.account.service }}
  type: {{ coalesce .Values.account.service.type .Values.common.service.type }}
  {{- else }}
  type: {{ .Values.common.service.type }}
  {{- end }}
  ports:
    - name: service
      port: {{ .Values.ports.account }}
      targetPort: service
      protocol: TCP
  selector:
    app.kubernetes.io/name: {{ .Chart.Name }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    component: account
