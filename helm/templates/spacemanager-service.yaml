apiVersion: v1
kind: Service
metadata:
  name: {{ include "databench.fullname" . }}-spacemanager
  labels:
{{ include "dataworkbench.labels" . | indent 4 }}
    component: spacemanager-service
spec:
  {{- if .Values.spacemanager.service }}
  type: {{ coalesce .Values.spacemanager.service.type .Values.common.service.type }}
  {{- else }}
  type: {{ .Values.common.service.type }}
  {{- end }}
  ports:
    - name: service
      port: {{ .Values.ports.spaceManager }}
      targetPort: service
      protocol: TCP
    - name: metrics
      port: {{ .Values.ports.spaceManagerMetrics }}
      targetPort: metrics
      protocol: TCP
  selector:
    app.kubernetes.io/name: {{ .Chart.Name }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    component: spacemanager
