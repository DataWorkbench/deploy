apiVersion: v1
kind: Service
metadata:
  name: {{ include "databench.fullname" . }}-jobmanager
  labels:
{{ include "dataworkbench.labels" . | indent 4 }}
    component: jobmanager-service
spec:
  {{- if .Values.jobmanager.service }}
  type: {{ coalesce .Values.jobmanager.service.type .Values.common.service.type }}
  {{- else }}
  type: {{ .Values.common.service.type }}
  {{- end }}
  ports:
    - name: service
      port: {{ .Values.ports.jobmanager }}
      targetPort: service
      protocol: TCP
  selector:
    app.kubernetes.io/name: {{ .Chart.Name }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    component: jobmanager
