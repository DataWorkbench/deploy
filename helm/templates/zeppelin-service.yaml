{{- if .Values.zeppelin.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "databench.fullname" . }}-zeppelin
  labels:
{{ include "dataworkbench.labels" . | indent 4 }}
    component: zeppelin-service
spec:
  {{- if .Values.zeppelin.service }}
  type: {{ coalesce .Values.zeppelin.service.type .Values.common.service.type }}
  {{- else }}
  type: {{ .Values.common.service.type }}
  {{- end }}
  ports:
    - name: service
      port: {{ .Values.ports.zeppelin }}
      targetPort: service
      protocol: TCP
  selector:
    app.kubernetes.io/name: {{ .Chart.Name }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    component: zeppelin
{{- end }}