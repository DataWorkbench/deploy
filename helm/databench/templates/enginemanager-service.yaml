apiVersion: v1
kind: Service
metadata:
  name: {{ include "dataomnis.fullname" . }}-enginemanager
  labels:
{{ include "dataomnis.labels" . | indent 4 }}
    component: enginemanager-service
spec:
  type: ClusterIP
  ports:
    - name: service
      port: {{ .Values.ports.enginemanager }}
      targetPort: service
      protocol: TCP
    - name: metrics
      port: {{ .Values.metricsPorts.enginemanager }}
      targetPort: metrics
      protocol: TCP
  selector:
    app.kubernetes.io/name: {{ .Chart.Name }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    component: enginemanager
