apiVersion: v1
kind: Service
metadata:
  name: {{ include "dataomnis.fullname" . }}-flowmanager
  labels:
{{ include "dataomnis.labels" . | indent 4 }}
    component: flowmanager-service
spec:
  type: ClusterIP
  ports:
    - name: service
      port: {{ .Values.ports.flowmanager }}
      targetPort: service
      protocol: TCP
    - name: metrics
      port: {{ .Values.metricsPorts.flowmanager }}
      targetPort: metrics
      protocol: TCP
  selector:
    app.kubernetes.io/name: {{ .Chart.Name }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    component: flowmanager
