apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "databench.fullname" . }}-global-ingress
  annotations:
    # limit the size of uploaded-file
    nginx.org/client-max-body-size: "100m"
spec:
  ingressClassName: nginx
  rules:
    - host: global.{{ .Values.domain }}
      http:
        paths:
        - path: /
          pathType: Prefix
          backend:
            service:
              name: {{ include "databench.fullname" . }}-apiglobal
              port:
                number: {{ .Values.ports.apiglobal }}
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "databench.fullname" . }}-jaeger-ingress
  annotations:
spec:
  ingressClassName: nginx
  rules:
    - host: jaeger.{{ .Values.domain }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ include "databench.fullname" . }}-jaeger
                port:
                  number: {{ .Values.ports.jaegerweb }}

---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "databench.fullname" . }}-zeppelin-ingress
  annotations:
    # support websocket
    nginx.org/websocket-services: {{ include "databench.fullname" . }}-zeppelin-server
spec:
  ingressClassName: nginx
  rules:
    - host: zeppelin.{{ .Values.domain }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ include "databench.fullname" . }}-zeppelin-server
                port:
                  number: {{ .Values.ports.zeppelin }}
