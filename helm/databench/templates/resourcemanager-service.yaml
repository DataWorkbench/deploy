apiVersion: v1
kind: Service
metadata:
  name: {{ include "dataomnis.fullname" . }}-resourcemanager
  labels:
{{ include "dataomnis.labels" . | indent 4 }}
    component: resourcemanager-service
spec:
  type: ClusterIP
  ports:
    - name: service
      port: {{ .Values.ports.resourcemanager }}
      targetPort: service
      protocol: TCP
    - name: metrics
      port: {{ .Values.metricsPorts.resourcemanager }}
      targetPort: metrics
      protocol: TCP
  selector:
    app.kubernetes.io/name: {{ .Chart.Name }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    component: resourcemanager
