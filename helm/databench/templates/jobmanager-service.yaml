apiVersion: v1
kind: Service
metadata:
  name: {{ include "databench.fullname" . }}-jobmanager
  labels:
{{ include "databench.labels" . | indent 4 }}
    component: jobmanager-service
spec:
  type: ClusterIP
  ports:
    - name: service
      port: {{ .Values.ports.jobManager }}
      targetPort: service
      protocol: TCP
    - name: metrics
      port: {{ .Values.ports.jobManagerMetrics }}
      targetPort: metrics
      protocol: TCP
  selector:
    app.kubernetes.io/name: {{ .Chart.Name }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    component: jobmanager
