apiVersion: v1
kind: Service
metadata:
  name: {{ include "dataomnis.fullname" . }}-account
  labels:
{{ include "dataomnis.labels" . | indent 4 }}
    component: account-service
spec:
  type: ClusterIP
  ports:
    - name: service
      port: {{ .Values.ports.account }}
      targetPort: service
      protocol: TCP
    - name: metrics
      port: {{ .Values.metricsPorts.account }}
      targetPort: metrics
      protocol: TCP
  selector:
    app.kubernetes.io/name: {{ .Chart.Name }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    component: account
