apiVersion: v1
kind: Service
metadata:
  name: {{ include "dataomnis.fullname" . }}-spacemanager
  labels:
{{ include "dataomnis.labels" . | indent 4 }}
    component: spacemanager-service
spec:
  type: ClusterIP
  ports:
    - name: service
      port: {{ .Values.ports.spacemanager }}
      targetPort: service
      protocol: TCP
    - name: metrics
      port: {{ .Values.metricsPorts.spacemanager }}
      targetPort: metrics
      protocol: TCP
  selector:
    app.kubernetes.io/name: {{ .Chart.Name }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    component: spacemanager
