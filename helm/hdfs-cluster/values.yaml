## FirstAtAll: create namenode / datanode / journalnode / zookeeper dir with: {{ .Values.hdfsHome }}/{{ .Release.Name }}/xxxxnode

fullnameOverride: ""

storageClass: ""

hdfsHome: /data/databench

version: 2.9.2

image: dataworkbench/hadoop:2.9.2   #  dataworkbench/hdfs-metrics:3.1.0

namenode:
  nodePort: 30091
  webPort: 50070  # 3.x 9870 ; 2.x 50070
  nodes:
    - "worker-1"
    - "worker-3"
  storage:
    capacity: 10Gi
    reclaimPolicy: Retain

datanode:
  nodes:
    - "worker-1"
    - "worker-2"
    - "worker-3"
  storage:
    capacity: 50Gi
    reclaimPolicy: Retain

journalnode:
  nodes:
    - "worker-1"
    - "worker-2"
    - "worker-3"
  storage:
    capacity: 10Gi
    reclaimPolicy: Retain

zookeeper:
  quorum:
  nodes:
    - "worker-1"
    - "worker-2"
    - "worker-3"
  storage:
    capacity: 10Gi
    reclaimPolicy: Retain

yarn:
  nmReplicas: ""  #  fill if you need to deploy yarn